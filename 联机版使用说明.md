# 谁是卧底联机版本地开发

## 1. 配置 DeepSeek Key

复制 `.env.example` 为 `.env`，填写：

```env
DEEPSEEK_API_KEY=你的 DeepSeek API Key
HOST=0.0.0.0
PORT=8000
```

## 2. 启动服务

```powershell
python server.py
```

电脑本机访问：

```text
http://127.0.0.1:8000
```

手机同 Wi-Fi 访问：

```text
http://电脑局域网IP:8000
```

## 常见问题

如果手机加入电脑创建的房间时提示“房间不存在”，检查 `.env`：

```env
HOST=0.0.0.0
```

不要设置成 `HOST=127.0.0.1`。`127.0.0.1` 只允许电脑自己访问，手机会进不到同一个服务。

刷新浏览器时，页面会用浏览器本地保存的 `clientId` 自动恢复原玩家，不会新增一个玩家。

房主需要设置“玩家人数”。开始游戏时，后端会校验实际加入人数是否等于设置人数；不一致时不会发牌。

## 3. 部署到公网

后续部署到服务器时，把 `server.py`、`谁是卧底1.3.html`、`.env` 放到服务器，运行：

```bash
python server.py
```

公网建议再用 Nginx 配 HTTPS。
